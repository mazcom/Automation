# from - базовый образ, с которого мы начинаем сборку. 3.6 - это тег. 
FROM python:3.10

# RUN определяет, что нужно выполнить определённую команду
RUN mkdir -p /usr/src/app/

# WORKDIR мы просто переходит в заданный каталог и команды выполняются с него.
WORKDIR /usr/src/app/

# принимает два аргументы. Откуда- куда. Точка - это текущая дириктория. В данном случае с нашей машины в контейнер.  
COPY . /usr/src/app/

# CMD это команда, что нужно делать когда мы запустим контейнер(docker start или run) 
CMD ["python", "app.py"]

# есть ещё ENTRYPOINT. CMD vs ENTRYPOINT. они выполняют похожие вещи. CMD запускает через shell. а ENTRYPOINT без shell.    
# ENTRYPOINT ["python", "app.py"]


